# Lean runtime for AgroCloud geoai-inference (Mask R-CNN extractors).
# Prefer a short venv path (e.g. C:\geoai-venv) — OneDrive paths hit Windows MAX_PATH on torch.
# Install torch first (CPU example):
#   pip install --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
# geoai-py --no-deps + these packages; app stubs leafmap to skip Jupyter/leafmap stack
geoai-py>=0.40.0
torchgeo>=0.5.0
geopandas>=0.14.0
rasterio>=1.3.0
xarray>=2024.1.0
rioxarray>=0.15.0
huggingface_hub>=0.20.0
scipy>=1.10.0
matplotlib>=3.7.0
opencv-python-headless>=4.8.0
albumentations>=1.3.0
scikit-image>=0.22.0
numpy>=1.24.0
pillow>=10.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
tqdm>=4.66.0

